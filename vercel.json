{
  "buildCommand": "rm -rf package-lock.json node_modules && npm install --include=optional && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "env": {
    "NODE_ENV": "production"
  }
}
